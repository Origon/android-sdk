<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="80dp"
    android:layout_height="80dp"
    android:layout_marginEnd="8dp">

    <androidx.cardview.widget.CardView
        android:layout_width="80dp"
        android:layout_height="80dp"
        app:cardBackgroundColor="@color/origon_remote_bubble"
        app:cardCornerRadius="10dp"
        app:cardElevation="0dp">

        <ImageView
            android:id="@+id/tile_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@null"
            android:scaleType="centerCrop"
            android:visibility="gone" />

        <LinearLayout
            android:id="@+id/tile_file"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="4dp">

            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:contentDescription="@null"
                android:src="@drawable/ic_endpoint"
                app:tint="@color/origon_text_secondary" />

            <TextView
                android:id="@+id/tile_filename"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:ellipsize="middle"
                android:gravity="center"
                android:maxLines="1"
                android:textColor="@color/origon_text_secondary"
                android:textSize="10sp" />
        </LinearLayout>

        <!-- Upload progress / error overlay -->
        <View
            android:id="@+id/tile_overlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#59000000"
            android:visibility="gone" />

        <ProgressBar
            android:id="@+id/tile_progress"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:max="100"
            android:progressTint="@color/white"
            android:visibility="gone" />

        <ImageView
            android:id="@+id/tile_error"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_gravity="center"
            android:contentDescription="@null"
            android:src="@drawable/ic_error"
            android:visibility="gone"
            app:tint="@color/white" />
    </androidx.cardview.widget.CardView>

    <!-- Remove (×) -->
    <ImageView
        android:id="@+id/tile_remove"
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:layout_gravity="top|end"
        android:layout_margin="2dp"
        android:background="@drawable/bg_circle_red"
        android:contentDescription="@string/attach_remove"
        android:padding="3dp"
        android:src="@drawable/ic_cross_small"
        app:tint="@color/white" />
</FrameLayout>
